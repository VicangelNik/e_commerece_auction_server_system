services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=auction-db'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3309:3306'
